import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Wallet | Test Stakewolle",
};

export default function WalletLayout({ children }: Children) {
  return children;
}
